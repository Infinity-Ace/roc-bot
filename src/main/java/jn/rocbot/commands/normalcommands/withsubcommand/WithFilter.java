package jn.rocbot.commands.normalcommands.withsubcommand;

public class WithFilter {
    public WithProperty[] properties;

    public WithFilter(WithProperty[] properties) {
        this.properties = properties;
    }
}
